import React from "react";
import { Colors, View, Text, TouchableOpacity, UIComponents } from 'react-native-ui-lib';

UIComponents.defaultProps = {};
// UIComponents.defaultProps.onError = error => console.error(error);
// rUIComponents.defaultProps.renderError = <View><Text> A component failed to render! </Text></View>;